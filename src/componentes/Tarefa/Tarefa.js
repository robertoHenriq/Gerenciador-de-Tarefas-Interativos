const Tarefa = ({Titulo, prioridade, descricao, time, responsavel, limite }) =>{

    return(
        <div>
            <h2>Tarefa{Titulo}</h2>
        </div>

    )
}

export default Tarefa